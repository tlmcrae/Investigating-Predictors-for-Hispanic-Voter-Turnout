* Running Descriptive Statistics
tab vot
tab educ
sum hufaminc
sum age 

* Visualizing 
graph pie, over(vot)

graph box age, over(vot)

graph bar, over(vot) over (educ) stack asyvars percentage

graph box hufaminc, over(vot)

graph box age, over(vot)

